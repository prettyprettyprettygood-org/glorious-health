# Static-first template default
# Add project-specific environment variables only when explicitly needed.

# Password gate for /admin — set this in your build/hosting environment (Netlify/Vercel/etc.),
# never commit the real value. This is a light deterrent, not real auth: the value ships in the
# client-side bundle for anyone who views source.
PUBLIC_ADMIN_PASSWORD=
